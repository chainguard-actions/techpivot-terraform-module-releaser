name: Test invalid default-first-tag config validation
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Run action with invalid default-first-tag (expect failure)
        id: action
        uses: .
        continue-on-error: true
        with:
          major-keywords: major change,breaking change
          minor-keywords: feat,feature
          patch-keywords: fix,chore,docs
          default-first-tag: "invalid-tag"
          terraform-docs-version: v0.20.0
          delete-legacy-tags: "true"
          disable-wiki: "true"
          wiki-sidebar-changelog-max: "5"
          disable-branding: "false"
          module-change-exclude-patterns: ".gitignore,*.md,*.tftest.hcl,tests/**"
          module-asset-exclude-patterns: ".gitignore,*.md,*.tftest.hcl,tests/**"
          use-ssh-source-format: "false"
          tag-directory-separator: /
          use-version-prefix: "true"

      - name: Assert action failed due to invalid first tag format
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" != "failure" ]; then
            echo "FAIL: expected action to fail with invalid default-first-tag format, but got outcome: $OUTCOME"
            exit 1
          fi
          echo "PASS: action correctly failed with invalid default-first-tag format"
